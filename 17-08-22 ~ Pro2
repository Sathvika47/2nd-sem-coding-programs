//C program to generate fibonacci series below 100 using arrays
#include <stdio.h>

int main()
{
    int a[15],i;
    a[0]=0;
    a[1]=1;
    for(i=2;i<15;i++)
    {
      a[i]=a[i-1]+a[i-2];
    }
    for(i=0;i<15;i++)
    {
        if(a[i]<100)
        printf("%d\t",a[i]);
    }
    return 0;
}
