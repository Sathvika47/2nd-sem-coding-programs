// C program to find the Factorial of a number using functions with return value

#include<stdio.h>
int fact(int);
void main() {
    int n,ans;
    printf("enter the number:");
    scanf("%d",&n);
    ans=fact(n);
    printf("Factorial of %d is %d",n,ans);
}
int fact(int n)
{
    int i,fact=1;
    for(i=1;i<=n;i++)
    fact=fact*i;
    return(fact);
}

Output:
enter the number:4
Factorial of 4 is 24
